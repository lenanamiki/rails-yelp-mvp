<div class="container">
  <h1>All Restaurants</h1>
    <% @restaurants.each do |restaurant| %>
      <div class="card">
        <h3><%= restaurant.name %><em>(<%= restaurant.category %>)</em></h3>
        <p><%= restaurant.address%></p>
        <p><%= restaurant.phone_number%></p>
      </div>
    <% end %>
</div>
